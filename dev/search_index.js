var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"This page contains the detailed docstrings for all exported types and functions in the SpectralClustering.jl package.\n\n","title":"API Reference"},{"category":"section","location":"api/#Functions-and-Types","page":"API Reference","text":"","title":"Functions and Types"},{"category":"type","location":"api/#SpectralClustering.KMeansDiscretization","page":"API Reference","text":"KMeansDiscretization(normalize_samples::Bool)\n\nIf normalize_samples is true, eigenvectors are normalized per sample before K-Means.\n\n\n\n\n\n","title":"SpectralClustering.KMeansDiscretization"},{"category":"type","location":"api/#SpectralClustering.LocalScaling","page":"API Reference","text":"LocalScaling(k::Int)\n\nLocal scaling affinity where scale depends on the k-th nearest neighbor.\n\n\n\n\n\n","title":"SpectralClustering.LocalScaling"},{"category":"type","location":"api/#SpectralClustering.RBFKernel","page":"API Reference","text":"RBFKernel(; sigma=1.0)\n\nGaussian kernel for affinity. sigma must be positive.\n\n\n\n\n\n","title":"SpectralClustering.RBFKernel"},{"category":"type","location":"api/#SpectralClustering.RandomWalkLaplacian","page":"API Reference","text":"Random Walk Normalized Laplacian: L_rw = I - D^{-1}W \n\n\n\n\n\n","title":"SpectralClustering.RandomWalkLaplacian"},{"category":"type","location":"api/#SpectralClustering.SVDDiscretization","page":"API Reference","text":"Yu & Shi method (Optimal multiclass using SVD) \n\n\n\n\n\n","title":"SpectralClustering.SVDDiscretization"},{"category":"type","location":"api/#SpectralClustering.SelfTuningDiscretization","page":"API Reference","text":"Zelnik-Manor & Perona method (Automatic k, cost minimization) \n\n\n\n\n\n","title":"SpectralClustering.SelfTuningDiscretization"},{"category":"type","location":"api/#SpectralClustering.SymmetricLaplacian","page":"API Reference","text":"Symmetric Normalized Laplacian: L_sym = I - D^{-1/2} W D^{-1/2} \n\n\n\n\n\n","title":"SpectralClustering.SymmetricLaplacian"},{"category":"type","location":"api/#SpectralClustering.UnnormalizedLaplacian","page":"API Reference","text":"Unnormalized Graph Laplacian: L = D - W \n\n\n\n\n\n","title":"SpectralClustering.UnnormalizedLaplacian"},{"category":"method","location":"api/#SpectralClustering.compute_affinity-Tuple{AbstractMatrix, RBFKernel}","page":"API Reference","text":"compute_affinity(X, method::RBFKernel; self_affinity=0.0)\n\nCompute the RBF affinity matrix. The affinity between two samples i and j is  computed as:\n\nexp(-||xᵢ - xⱼ||² / (2σ²))\n\nwhere σ is defined in the RBFKernel struct.\n\nArguments\n\nX: Data matrix with shape n_features × n_samples. Each column is one sample.\nmethod: RBF kernel configuration containing the width parameter sigma.\nself_affinity: Value used on the diagonal of the affinity matrix.\n\nReturns\n\nA symmetric n_samples × n_samples affinity matrix.\n\n\n\n\n\n","title":"SpectralClustering.compute_affinity"},{"category":"method","location":"api/#SpectralClustering.compute_eigenvectors-Tuple{AbstractMatrix, Int64}","page":"API Reference","text":"compute_eigenvectors(L::AbstractMatrix, k::Int)\n\nExtracts the bottom k eigenvectors from the Laplacian L.\n\nThis function computes the eigenvector embedding used in spectral clustering. The input L is expected to be a graph Laplacian or a normalized graph Laplacian.\n\nArguments\n\nL: Square Laplacian matrix.\nk: Number of eigenvectors to return.\n\nReturns\n\nA matrix whose rows are the selected eigenvectors. The matrix has one column per sample and k rows (features × samples format).\n\nThrows\n\nArgumentError if L is not square.\nArgumentError if k is smaller than 1 or larger than the number of samples.\nArgumentError if the selected eigenvectors contain non-negligible imaginary parts.\n\n\n\n\n\n","title":"SpectralClustering.compute_eigenvectors"},{"category":"method","location":"api/#SpectralClustering.compute_laplacian-Tuple{AbstractMatrix, RandomWalkLaplacian}","page":"API Reference","text":"compute_laplacian(W::AbstractMatrix, ::RandomWalkLaplacian)\n\nCompute the random-walk normalized graph Laplacian from an affinity matrix W.\n\nThe random-walk normalized Laplacian is defined as\n\nL_rw = I - D⁻¹W\n\nwhere W is the affinity matrix and D is the degree matrix.\n\nThis implementation is based on the normalized cuts formulation by Shi and Malik. Their paper formulates the problem as the generalized eigenvalue problem\n\n(D - W)y = λDy\n\nwith\n\nL = D - W\n\nMultiplying both sides by D⁻¹ gives\n\nD⁻¹(D - W)y = λy\n\nand therefore\n\n(I - D⁻¹W)y = λy\n\nArguments\n\nW: Square symmetric affinity matrix. Entry W[i, j] stores the similarity between samples i and j.\n::RandomWalkLaplacian: Selects the random-walk normalized Laplacian.\n\nReturns\n\nThe random-walk normalized Laplacian matrix L_rw.\n\nThrows\n\nArgumentError if W is not square.\nArgumentError if W is not symmetric.\nArgumentError if W contains negative affinity values.\nArgumentError if at least one node has degree zero.\n\n\n\n\n\n","title":"SpectralClustering.compute_laplacian"},{"category":"method","location":"api/#SpectralClustering.compute_laplacian-Tuple{AbstractMatrix, SymmetricLaplacian}","page":"API Reference","text":"function compute_laplacian(W::AbstractMatrix, ::SymmetricLaplacian)\n\nComputes the symmetric normalized Laplacian of an input matrix `W`. \n\nThe symmetric normalized Laplacian is defined as:\n\n    L_sym = I - D^{-1/2} W D^{-1/2}\n\nwith \n\n    D being the degree Matrix with the degree of a node being the sum of all the (affinity) values in its row and\n        D^{-1/2} being the square root of its inverse, and\n\n    W being the input Matrix (in our case the affinity Matrix)\n\nArguments\n\nW: Input matrix. In our case an affinity Matrix in which entry W[i,j] describes how similar points i and j are   W has to be square and symmetric and must not contain any negative values. Furthermore it must not contain zero degree nodes.\n::SymmetricLaplacian: Selects the symmetric normalized Laplacian.\n\nReturns\n\nThe symmetric normalized Laplacian L.\n\nThe specific type of the Laplacian depends on the type of the input matrix W. It will, however, always be a subtype of AbstractMatrix.     In our implementation the function will always be called with an affinity matrix of type Matrix{Float64}.     In such a case the output will also be of that type.\n\nThrows\n\nArgumentError if W is not square.\nArgumentError if W is not symmetric.\nArgumentError if W contains negative values.\nArgumentError if W contains at least one zero degree node.\n\n\n\n\n\n","title":"SpectralClustering.compute_laplacian"},{"category":"method","location":"api/#SpectralClustering.discretize-Tuple{AbstractMatrix, KMeansDiscretization}","page":"API Reference","text":"discretize(V::AbstractMatrix, method::KMeansDiscretization; k::Union{Int, Nothing}=nothing)\n\nDiscretize a spectral embedding into cluster labels using K-Means.\n\nThe input matrix V is the eigenvector embedding produced by spectral clustering. Each column of V represents one sample, and each row represents one selected eigenvector.\n\nK-Means is applied to the columns of V, so samples with similar coordinates in the spectral embedding are assigned to the same cluster.\n\nIf method.normalize_samples is true, each column of V is normalized to unit length before K-Means is applied. This is used for variants such as the Ng-Jordan-Weiss normalized spectral clustering method.\n\nArguments\n\nV: Spectral embedding matrix with one column per sample (features × samples).\nmethod: K-Means discretization configuration.\nk: Number of clusters.\n\nReturns\n\nA vector of cluster labels with one label per sample.\n\nThrows\n\nArgumentError if k is not provided.\nArgumentError if k is smaller than 1 or larger than the number of samples.\nArgumentError if row normalization is requested and at least one row has norm zero.\n\n\n\n\n\n","title":"SpectralClustering.discretize"},{"category":"function","location":"api/#SpectralClustering.make_blobs","page":"API Reference","text":"make_blobs([rng::AbstractRNG,] n_samples::Int=100; centers::Int=3, cluster_std::Real=1.0, center_box::Tuple{Real, Real}=(-10.0, 10.0))\n\nGenerate isotropic Gaussian blobs for clustering.\n\nArguments\n\nrng: Optional random number generator (defaults to Random.default_rng()).\nn_samples: Total number of points.\ncenters: Number of clusters.\ncluster_std: Standard deviation of the clusters.\ncenter_box: The bounding box (min, max) for generating random cluster centers.\n\nReturns\n\nX: A 2 × n_samples Matrix of Float64.\ny: A n_samples Vector of Int (cluster labels 1 to centers).\n\n\n\n\n\n","title":"SpectralClustering.make_blobs"},{"category":"function","location":"api/#SpectralClustering.make_circles","page":"API Reference","text":"make_circles([rng::AbstractRNG,] n_samples::Int=100; noise::Float64=0.0, factor::Float64=0.8)\n\nGenerate a large circle containing a smaller circle in 2D.\n\nArguments\n\nrng: Optional random number generator (defaults to Random.default_rng()).\nn_samples: Total number of points to generate (split equally between the two circles).\nnoise: Standard deviation of Gaussian noise added to the data.\nfactor: Scale factor between inner and outer circle (radius of inner circle / radius of outer circle).\n\nReturns\n\nX: A 2 × n_samples Matrix of Float64.\ny: A n_samples Vector of Int (cluster labels 1 and 2).\n\n\n\n\n\n","title":"SpectralClustering.make_circles"},{"category":"function","location":"api/#SpectralClustering.make_moons","page":"API Reference","text":"make_moons([rng::AbstractRNG,] n_samples::Int=100; noise::Float64=0.0)\n\nGenerate two interleaving half circles in 2D.\n\nArguments\n\nrng: Optional random number generator (defaults to Random.default_rng()).\nn_samples: Total number of points.\nnoise: Standard deviation of Gaussian noise.\n\nReturns\n\nX: A 2 × n_samples Matrix of Float64.\ny: A n_samples Vector of Int (cluster labels 1 and 2).\n\n\n\n\n\n","title":"SpectralClustering.make_moons"},{"category":"method","location":"api/#SpectralClustering.spectral_cluster-Tuple{AbstractMatrix, Int64}","page":"API Reference","text":"spectral_cluster(X, k; affinity, laplacian, discretizer)\n\nPerform spectral clustering on data matrix X into k clusters. X should be n_features × n_samples.\n\n\n\n\n\n","title":"SpectralClustering.spectral_cluster"},{"category":"section","location":"manual/eigensolvers/#Eigensolvers","page":"Eigensolvers","text":"After constructing the graph Laplacian, the core of Spectral Clustering is to compute its eigenvalues and eigenvectors. The first k eigenvectors corresponding to the smallest eigenvalues contain the structural information needed to partition the graph into k clusters.\n\nSpectralClustering.jl provides the compute_eigenvectors function to seamlessly extract these vectors, returning them in a features × samples format (so the rows correspond to the eigenvectors).\n\nusing SpectralClustering\n\n# Compute the first k eigenvectors\nV = compute_eigenvectors(L, k)","title":"Eigensolvers"},{"category":"section","location":"manual/eigensolvers/#Visualizing-the-Eigenvectors","page":"Eigensolvers","text":"Why do we compute the eigenvectors in the first place? \n\nThe original dataset might be tangled and non-linearly separable (like two concentric circles or interlocking moons). The eigenvectors map our data into a new k-dimensional space (often called the spectral embedding). In this new space, the complex geometric structures are \"unrolled\" and the clusters become simply linearly separable points that standard K-Means can easily cluster.\n\nLet's visualize this transformation!\n\nusing SpectralClustering\nusing Plots\nusing LinearAlgebra\nusing Random\n\n# 1. Generate interlocking moons (non-linearly separable)\nrng = Xoshiro(42)\nX, y = make_moons(rng, 300; noise=0.05)\n\n# 2. Compute Affinity and Laplacian\nW = compute_affinity(X, RBFKernel(sigma=0.1))\nL = compute_laplacian(W, RandomWalkLaplacian())\n\n# 3. Compute eigenvalues and eigenvectors\n# We'll use LinearAlgebra.eigen here so we can also look at the eigenvalues!\n# (In your own code, you can use `V = compute_eigenvectors(L, 3)` to just get the vectors).\nF = eigen(L)\nidx = sortperm(real.(F.values))\nλ = real.(F.values[idx])\nV = real.(F.vectors[:, idx])\n\n# 4. Plot the Eigenvalues\n# The \"Eigengap\" heuristic tells us the ideal number of clusters.\n# A large jump between eigenvalue k and k+1 suggests k clusters.\np1 = bar(1:8, λ[1:8], \n    title=\"Bottom 8 Eigenvalues\",\n    xlabel=\"Index\", \n    ylabel=\"Eigenvalue\",\n    legend=false,\n    color=:coral\n)\n\n# 5. Plot the data in the Spectral Embedding (2nd vs 3rd eigenvector)\np2 = scatter(V[:, 2], V[:, 3], \n    group=y, \n    markerstrokewidth=0,\n    markersize=4,\n    title=\"Spectral Embedding\",\n    xlabel=\"2nd Eigenvector\",\n    ylabel=\"3rd Eigenvector\",\n    legend=:topright\n)\n\nplot(p1, p2, layout=(1, 2), size=(800, 400), framestyle=:box, margin=5Plots.mm)","title":"Visualizing the Eigenvectors"},{"category":"section","location":"manual/eigensolvers/#1.-The-Eigengap-(Left-Plot)","page":"Eigensolvers","text":"Notice that the bars for the first two eigenvalues (k=1 and k=2) are so small they are barely visible, essentially being zero. This mathematically confirms that there are two distinct structural components (clusters) in our data. The massive vertical jump (the \"Eigengap\") right after the 2nd eigenvalue acts as a perfect mathematical signal telling us we should configure our algorithm to search for k=2 clusters.","title":"1. The Eigengap (Left Plot)"},{"category":"section","location":"manual/eigensolvers/#2.-The-Spectral-Embedding-(Right-Plot)","page":"Eigensolvers","text":"As you can see, when we map the data into the new space created by the 2nd and 3rd eigenvectors, the original tangled moons are pulled apart into two tight, isolated clusters that are now completely linearly separable!","title":"2. The Spectral Embedding (Right Plot)"},{"category":"section","location":"manual/affinities/#Affinities","page":"Affinities","text":"The first step in spectral clustering is to construct a Similarity Graph (or Affinity Matrix) from the raw data points. This matrix represents how \"close\" or \"similar\" every point is to every other point.\n\nSpectralClustering.jl provides different kernels to compute these affinities.","title":"Affinities"},{"category":"section","location":"manual/affinities/#RBF-Kernel","page":"Affinities","text":"The Radial Basis Function (RBF) or Gaussian kernel is the most standard affinity measure. The similarity between two points x_i and x_j is given by:\n\nW_ij = expleft(-fracx_i - x_j^22sigma^2right)\n\nusing SpectralClustering\n\n# Using a standard RBF Kernel with sigma = 0.2\nkernel = RBFKernel(sigma=0.2)","title":"RBF Kernel"},{"category":"section","location":"manual/affinities/#Local-Scaling","page":"Affinities","text":"The LocalScaling kernel dynamically adjusts the bandwidth (sigma) for each individual point based on the distance to its k-th nearest neighbor. This is highly effective for datasets with varying densities.\n\n# Using Local Scaling based on the 7th nearest neighbor\nkernel_local = LocalScaling(7)","title":"Local Scaling"},{"category":"section","location":"manual/affinities/#Visualizing-the-Affinity-Graph","page":"Affinities","text":"Because an affinity matrix W is just an adjacency matrix of a weighted graph, we can visualize the connections between points! Here, we generate a \"moons\" dataset, compute the affinity matrix, and plot it. \n\nWe threshold the matrix to only keep strong connections (e.g., W_ij  01), revealing the underlying manifold structure that the algorithm relies on.\n\nusing SpectralClustering\nusing Plots\nusing GraphRecipes\nusing Random\n\n# 1. Generate data\nrng = Xoshiro(42)\nX, y = make_moons(rng, 150, noise=0.05)\n\n# 2. Compute the affinity matrix using an RBF Kernel\nW = compute_affinity(X, RBFKernel(sigma=0.2))\n\n# 3. Sparsify the matrix for cleaner visualization (remove weak edges)\nW_sparse = W .* (W .> 0.1)\n\n# 4. Plot the graph over the actual data coordinates\ngraphplot(\n    W_sparse, \n    x = X[1, :],           # True X coordinates\n    y = X[2, :],           # True Y coordinates\n    curves = false,        # Straight lines\n    nodeshape = :circle,\n    markercolor = :mediumseagreen,\n    nodesize = 0.3, \n    linewidth = 1.0,\n    linecolor = :gray,\n    linealpha = 0.3,\n    title = \"Affinity Graph Visualization\",\n    framestyle = :none,\n    legend = false,\n    size = (600, 400)\n)","title":"Visualizing the Affinity Graph"},{"category":"section","location":"getting_started/#Getting-Started","page":"Getting Started","text":"This guide will show you how to start using SpectralClustering.jl to perform clustering on non-linearly separable datasets.","title":"Getting Started"},{"category":"section","location":"getting_started/#Basic-Usage","page":"Getting Started","text":"Spectral clustering is particularly useful when the clusters in your data are not linearly separable (like circles or moons).  Let's see how SpectralClustering.jl performs on the \"moons\" dataset.\n\nusing SpectralClustering\nusing Plots\nusing Random\n\n# 1. Generate a non-linearly separable dataset\nrng = Xoshiro(42)\nX, y_true = make_moons(rng, 400, noise=0.05)\n\n# 2. Perform Spectral Clustering\n# We expect 2 clusters (k=2)\nk = 2\ny_pred = spectral_cluster(X, k)\n\n# 3. Plot the results\np1 = scatter(X[1,:], X[2,:], group=y_true, title=\"Ground Truth\", legend=false, markersize=3)\np2 = scatter(X[1,:], X[2,:], group=y_pred, title=\"Spectral Clustering\", legend=false, markersize=3)\n\nplot(p1, p2, layout=(1,2), size=(800, 400), margin=5Plots.mm)\n\n(Note: Because clustering is unsupervised, it assigns arbitrary integer labels to the groups it finds, meaning the predicted colors may appear swapped compared to the ground truth.)","title":"Basic Usage"},{"category":"section","location":"getting_started/#Customizing-the-Algorithm","page":"Getting Started","text":"You can customize the different steps of the spectral clustering algorithm (affinity matrix construction, graph laplacian type, and discretization method) by passing keyword arguments to the spectral_cluster function.\n\n# Using a different Laplacian and Kernel bandwidth\ny_pred_custom = spectral_cluster(\n    X, 2;\n    affinity = RBFKernel(sigma=0.1),\n    laplacian = RandomWalkLaplacian(),\n    discretizer = KMeansDiscretization(true) # true for normalize_rows\n)\n\nscatter(X[1,:], X[2,:], group=y_pred_custom, title=\"Custom Spectral Clustering\", legend=false, markersize=3)","title":"Customizing the Algorithm"},{"category":"section","location":"manual/laplacians/#Laplacians","page":"Laplacians","text":"Once we have constructed the Affinity Matrix W, the next step is to compute the Graph Laplacian.  The Laplacian matrix captures the structural properties of the graph, and its spectrum (eigenvalues and eigenvectors) is what we use to identify the clusters.\n\nSpectralClustering.jl provides three types of Laplacians:","title":"Laplacians"},{"category":"section","location":"manual/laplacians/#Unnormalized-Laplacian","page":"Laplacians","text":"The unnormalized Laplacian is defined as:\n\nL = D - W\n\nwhere D is the diagonal degree matrix with D_ii = sum_j W_ij.\n\nusing SpectralClustering\nL = compute_laplacian(W, UnnormalizedLaplacian())","title":"Unnormalized Laplacian"},{"category":"section","location":"manual/laplacians/#Random-Walk-Normalized-Laplacian","page":"Laplacians","text":"The random-walk normalized Laplacian is often the preferred choice for spectral clustering because of its connection to Markov chains. It is defined as:\n\nL_rw = I - D^-1 W\n\nL_rw = compute_laplacian(W, RandomWalkLaplacian())","title":"Random Walk Normalized Laplacian"},{"category":"section","location":"manual/laplacians/#Symmetric-Normalized-Laplacian","page":"Laplacians","text":"The symmetric normalized Laplacian is defined as:\n\nL_sym = I - D^-12 W D^-12\n\nL_sym = compute_laplacian(W, SymmetricLaplacian())","title":"Symmetric Normalized Laplacian"},{"category":"section","location":"manual/laplacians/#Visualizing-the-Matrix-Structure","page":"Laplacians","text":"We can visualize the structure of the matrices using a heatmap. If our data is perfectly clustered and the matrix rows/columns are ordered by their cluster label, the matrices will show a distinct block-diagonal structure!\n\nBecause the diagonal of the Random Walk Laplacian is exactly 1.0 while its off-diagonals are very small negative numbers, plotting the Laplacian directly often results in an image dominated entirely by the diagonal line. To actually see the block structure, we temporarily zero out the diagonal for visualization.\n\nusing SpectralClustering\nusing Plots\nusing LinearAlgebra\nusing Random\n\n# Generate highly separated blobs\nrng = Xoshiro(42)\nX, y = make_blobs(rng, 150; centers=3, cluster_std=0.5)\n\n# Compute Affinity and Laplacian\nW = compute_affinity(X, RBFKernel(sigma=1.0))\nL = compute_laplacian(W, RandomWalkLaplacian())\n\n# Sort nodes by their true cluster labels to reveal the block structure\nperm = sortperm(y)\nL_sorted = L[perm, perm]\n\n# Zero out the diagonal so it doesn't wash out the color scale\nL_vis = copy(L_sorted)\nL_vis[diagind(L_vis)] .= 0.0\n\nheatmap(L_vis, \n    yflip=true, \n    c=:viridis, \n    title=\"Random Walk Laplacian\\n(Diagonal removed for visibility)\",\n    size=(500, 500)\n)","title":"Visualizing the Matrix Structure"},{"category":"section","location":"manual/discretization/#Discretization","page":"Discretization","text":"","title":"Discretization"},{"category":"section","location":"manual/datasets/#Datasets","page":"Datasets","text":"SpectralClustering.jl comes with several built-in functions to generate synthetic datasets for testing clustering algorithms. These datasets are specifically designed to highlight scenarios where standard algorithms (like K-Means) fail, but spectral clustering succeeds.\n\nAll dataset generators return a tuple (X, y), where X is a 2 times N matrix of data points and y is a vector of the ground truth cluster assignments.","title":"Datasets"},{"category":"section","location":"manual/datasets/#Moons","page":"Datasets","text":"make_moons generates two interleaving half-circles. This is a classic example of a non-linearly separable dataset.\n\nusing SpectralClustering\nusing Plots\nusing Random\n\nrng = Xoshiro(42)\nX, y = make_moons(rng, 300, noise=0.05)\nscatter(X[1,:], X[2,:], group=y, legend=false, title=\"make_moons\", markersize=3, aspect_ratio=:equal)","title":"Moons"},{"category":"section","location":"manual/datasets/#Circles","page":"Datasets","text":"make_circles generates a large circle containing a smaller circle. Spectral clustering is one of the few techniques that can easily separate these concentric shapes.\n\nrng = Xoshiro(42)\nX, y = make_circles(rng, 300, noise=0.03, factor=0.5)\nscatter(X[1,:], X[2,:], group=y, legend=false, title=\"make_circles\", markersize=3, aspect_ratio=:equal)","title":"Circles"},{"category":"section","location":"manual/datasets/#Blobs","page":"Datasets","text":"make_blobs generates isotropic Gaussian blobs. This is a standard linearly separable dataset where almost any clustering algorithm performs well.\n\nrng = Xoshiro(42)\nX, y = make_blobs(rng, 300, centers=4, cluster_std=1.5)\nscatter(X[1,:], X[2,:], group=y, legend=false, title=\"make_blobs\", markersize=3, aspect_ratio=:equal)","title":"Blobs"},{"category":"section","location":"installation/#Installation","page":"Installation","text":"SpectralClustering.jl requires Julia v1.11 or higher.","title":"Installation"},{"category":"section","location":"installation/#Using-the-Package-Manager","page":"Installation","text":"To install the package directly from the GitHub repository, open the Julia REPL and press ] to enter Pkg mode. Then run:\n\npkg> add https://github.com/E-175/SpectralClustering.jl","title":"Using the Package Manager"},{"category":"section","location":"installation/#Local-Development","page":"Installation","text":"If you want to contribute to the package, view the source code, or run the included demos locally, you can clone the repository and instantiate the environment:\n\ngit clone https://github.com/E-175/SpectralClustering.jl\ncd SpectralClustering.jl\njulia --project=. -e 'using Pkg; Pkg.instantiate()'","title":"Local Development"},{"category":"section","location":"#SpectralClustering.jl","page":"Home","text":"A Julia package for performing non-convex and non-linearly separable spectral clustering.\n\n(Image: Dev) (Image: Build Status) (Image: Coverage)\n\nWelcome to the documentation for SpectralClustering.jl!  This package provides a modular pipeline for performing spectral clustering, including configurable affinity kernels, graph Laplacians, and discretization methods.\n\nTo get started, see the Getting Started guide.","title":"SpectralClustering.jl"}]
}
